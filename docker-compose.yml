services:
  app:
    build: .
    container_name: api
    restart: always
    ports:
      - "8081:8080"
    environment:
      MIW_API_URL: http://host.docker.internal:8081
    networks:
      - miw

networks:
  miw:
    external: true

#> docker compose up --build -d